using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SwitchBackground : MonoBehaviour
{
    /*[SerializeField]
    private GameObject backScenarioTwo;
    [SerializeField]
    private GameObject scenarioOne;

    void Update()
    {
        if (Mathf.FloorToInt(Player.score) > 100)
        {
           if((Mathf.FloorToInt(Player.score) % 100) == 0)
            {
                trocarScenario();
            }
        }
    }
    void trocarScenario()
    {
        if (backScenarioTwo.activeSelf)
        {
            backScenarioTwo.SetActive(false);
        }
        else
        {
            backScenarioTwo.SetActive(true);
        }
    }*/
}
